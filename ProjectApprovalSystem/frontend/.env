# Frontend environment variables for Vite
# Variables exposed to client-side code must start with VITE_
# Set VITE_API_URL to your backend API base (include /api if your backend uses that path).
# Example: VITE_API_URL=http://localhost:5000/api

VITE_API_URL=http://localhost:3000/api
